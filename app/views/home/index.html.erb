<div class="container mt-3">
  <div id="typed-strings">
    <h1 class="overflow-hidden">Adopt a Pet</h1>
    <h1 class="overflow-hidden">Donate a Pet</h1>
  </div>
  <h1 class="text-center typed-font" id="typed"></h1>
  <!-- <script>
    var typed = new Typed('#typed', {
      stringsElement: '#typed-strings',
      typeSpeed: 50,
      backSpeed: 50,
      smartBackspace: true,
      showCursor: false,
    });
  </script> -->

  <div class="cards">

    <% @pets.each do |pet| %>
      <a href="<%= "/pets/#{pet.id}" %>"> 
        <div class="card-category"
          style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= "https://source.unsplash.com/1600x900/?#{pet.animal_type}" %>)"> 
          <%= pet.name %>
        </div>
      </a>
    <% end %>
  </div>
</div>
